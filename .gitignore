node_modules/
uploads/
.env
# Transformers.js caches downloaded model weights
.cache/
/tests/test-input
/tests/test-output
.venv/
# scispaCy pins numpy 1.x, so the abbreviation extractor gets its own venv
# rather than downgrading the main one (see scripts/extract_abbreviations.py).
.venv-abbrev/
__pycache__/

# Built knowledge graphs (graph.json/.html and the pre-merge snapshot) are
# regenerated by the pipeline; only the model catalogue is source.
/documents/*
!/documents/model_metadata.json
# Input corpus for the synthesis runs — 192 PDFs, ~645 MB.
/tests/test-input-synthesis/

# Test artifacts + frontend build output
tests/test_log.txt
tests/test_token_usage.txt
tests/KG_log.txt
frontend/dist/

# Chat prompt/context/response log (backend/routes/chat.js)
chat_log.txt

# Vendored browser embedding model (npm run fetch:model)
models/

# Per-chat pipeline scratch (Postgres is canonical; these are stage exchange files)
data/collections/
# Output of the deprecated enhance_pdf.js stage — no longer in the pipeline
data/enhanced/
